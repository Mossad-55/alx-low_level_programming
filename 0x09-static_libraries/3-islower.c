#include "main.h"

/**
 * _islower -  returns 1 if it's lower otherwise 0.
 * @c: character as a param.
 * Return: int.
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
		return (1);

return (0);
}
