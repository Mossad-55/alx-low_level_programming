#include "main.h"

/**
 * add -  Returens the sum of two numbers.
 * @x: a param of type int. (first number)
 * @y: a param of type int. (second number)
 * Return: int.
 */
int add(int x, int y)
{
	return (x + y);
}
